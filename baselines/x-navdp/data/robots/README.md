Place robot USD assets here (not included in repo due to size/license):

- `dingo.usd` — wheeled robot (referenced by `src/environment/robots/dingo_config.py`)
- `unitreeg1.usd` — humanoid robot (referenced by `src/environment/robots/g1_config.py`)

Unitree Go2 uses Isaac Lab nucleus asset path by default.
