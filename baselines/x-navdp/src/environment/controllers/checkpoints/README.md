Place exported locomotion policies here (not included in repo):

- `humanoid_g1/policy.pt` — G1 velocity tracking policy (TorchScript)
- `quadruped_go2/policy.pt` — Go2 velocity tracking policy (TorchScript)
  (`go2/policy.pt` is also accepted as a fallback path)

Paths are resolved relative to this directory by default.
