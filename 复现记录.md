# NavDP 复现记录（Windows）

> 复现论文：NavDP（基于扩散策略的跨模态导航）
> 复现人：张巍苧（如身机器人实习）
> 时间：2026-08 起
> 说明：本分支专门记录复现流程、问题与结果，`master` 保持官方代码不动。

---

## 1. 环境配置（Windows 11 + RTX 4060 Laptop 8GB）

### 1.1 双环境架构（关键决策）

| 环境 | 位置 | 用途 | 关键包 |
|------|------|------|--------|
| navdp | C:\Users\zZzwn\anaconda3\envs\navdp | 跑模型服务器（HTTP） | torch 2.5.1+cu124 |
| isaacsim | D:\IsaacSim | 跑仿真评测 | torch 2.4.0+cu124, numpy 1.26.4, tensordict 0.13.0 |

- **为什么分两个环境**：模型和仿真对 torch 版本要求不同（2.5.1 vs 2.4.0），混在一起会互相破坏（见 2.4 事故）
- **仓库**：D:\NavDP（全部英文路径，中文路径会导致兼容问题）

### 1.2 版本精确锁定（不可升级！）

- IsaacSim **4.2.0.2**（NavDP 要求精确版本，官方 pip 安装）
- IsaacLab **v1.2.0**（git tag）
- 权重：checkpoints/navdp-cross-modal.ckpt（518MB）

### 1.3 场景资产（HuggingFace Scene-N1，gated 需登录同意条款）

- cluttered_easy / cluttered_hard（各 10 个杂乱场景，含起终点对 npy）
- Materials（5 类材质）+ SkyTexture（10 张天空 hdr）
- internscenes（74GB，PointGoal 大场景）**未下载**

---

## 2. 踩坑记录（7 道关卡）

### 2.1 缺 open3d
- 现象：`ModuleNotFoundError: No module named 'open3d'`
- 解决：`pip install open3d -i 阿里云镜像`

### 2.2 tensordict 进程内崩溃（最凶险）
- 现象：`Windows fatal exception: access violation`，崩在 tensordict._C
- 根因：IsaacSim 进程内 C 扩展**加载顺序冲突**（先启动 App 再 import tensordict 就崩）
- 解决：`eval_nogoal_wheeled.py` 第 2 行（AppLauncher 之前）提前 `import tensordict`

### 2.3 缺 rsl_rl
- 现象：`No module named 'rsl_rl'`（README 说可忽略，实际评测需要）
- 解决：`pip install git+https://github.com/leggedrobotics/rsl_rl.git --no-deps`（需代理）

### 2.4 tensordict 重装事故（血泪教训）
- 现象：`pip install --force-reinstall tensordict` 把 torch 偷偷升级到 2.13.0 CPU 版
- 教训：**装任何包必须 `--no-deps`**，否则 pip 依赖解析会动 torch/numpy
- 修复：`pip install torch==2.4.0 torchvision==0.19.0 --index-url .../cu124 --no-deps`

### 2.5 botocore 版本冲突
- 现象：replicator 加载失败（相机依赖），`cannot import DEFAULT_CHECKSUM_ALGORITHM`
- 根因：IsaacSim 自带旧 botocore（1.34.68）抢占 import，与新 s3transfer 不匹配
- 解决：pip_prebundle 里旧 botocore 改名 `botocore_old`

### 2.6 MP4 文件名冒号（Windows 特性）
- 现象：服务器写视频失败 `Error opening output ...09:32:49_fps_pointgoal.mp4`
- 根因：Windows 文件名**不能含冒号** `:`（Linux 可以），时间戳格式 `%H:%M:%S` 导致
- 解决：navdp_server.py 两处 `strftime` 改 `%H-%M-%S`
- 连锁反应：服务器 500 → 评测收不到轨迹 → 机器人不动

### 2.7 C 盘空间告急
- 现象：C 盘剩 23GB（93%）
- 元凶：pip 缓存占 23GB
- 解决：`pip cache purge`

---

## 3. 评测结果

### 3.1 100 回合统计（cluttered_easy 场景）

| 指标 | 复现结果 | 论文（1000 episodes） |
|------|---------|---------------------|
| 平均时间 | 30.5s | 106.2s |
| 平均面积 | 66.0 m² | 274.1 m² |
| 有效回合（>1s） | 32 个（32%） | — |
| 有效回合平均时间 | 95.1s | 106.2s |
| 最佳回合 | 120s / 276 m² | 274.1 m² |

**结论**：模型本身复现成功（最佳回合超论文平均）；秒挂率高拉低平均。

### 3.2 秒挂问题（68% 回合 <1s）

原因分两类：
1. **起点贴障碍物**（数据问题）：部分预定义起点离障碍物/边界太近，出生即碰撞
2. **深度传感器失效**（技术问题）：深度输出全 inf，模型感知距离失效

---

## 4. 当前问题：深度传感器输出 inf（待解决）

### 4.1 现象
- RGB 相机正常（亮度均值 130~150）
- 深度相机输出全 `inf`（无效值）
- 表现：机器人在障碍物前来回摇摆、越来越近、最终撞上

### 4.2 影响
- 轨迹可视化黑屏（深度反投影 invalid value）
- 模型感知距离失效 → 碰撞增多

### 4.3 论文对照（重要发现）
论文消融实验（Table V，PointNav）：
- **w/o Depth（去掉深度）**：SR 60.3 → 47.8（**−12.5**）
- w/o RGB（去掉 RGB）：SR 60.3 → 53.9（−6.4）

→ **深度贡献是 RGB 的两倍**。我们的深度失效意外复现了论文 w/o Depth 的退化现象，
反向验证了深度模态对导航的核心作用。

### 4.4 待办
- 查 IsaacLab headless 模式深度渲染要求（可能需要渲染器配置）
- 修复后重新评测，对比秒挂率

---

## 5. 复现产物清单

- 视频：D:\NavDP\nogoal_navdp_cluttered_easy\cluttered_easy\easy_0\（fps_N.mp4 原始 + fps_N_ok.mp4 转码播放版）
- 最佳回合：fps_86.mp4（120s / 276 m²）
- 成绩单：metric.csv（100 行）
- 截图：见 repro_images/

---

## 6. 给复现者的建议

1. 装包永远 `--no-deps`
2. Windows 文件名别用冒号
3. 改代码前先确认文件路径（仓库迁移后容易改错副本）
4. 评测 = 双窗口（服务器 + 仿真），报错常在对面的窗口
